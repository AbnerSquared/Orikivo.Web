// cssHandler: Handles how all of the css is read.

/*
// define class Color?
class SitePadding
{
    constructor(thin, standard, thick)
    {

    }
}

class WebFontSize
{
    constructor(smaller, small, standard, large, larger)
    {

    }
}

// definers stay here.
class ColorMap
{
    constructor(name, ...map)
    {
        this.name = name;
        this.map = map;
    }

    // designs the color map to format as CSS.
    toCss() { }
}
*/

// 1~8
// glow, bright, standard, dim, dark
// store color values as Hex || RGB